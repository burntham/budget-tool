<%= form_for(expense_detail) do |f| %>
  <% if expense_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expense_detail.errors.count, "error") %> prohibited this expense_detail from being saved:</h2>

      <ul>
      <% expense_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %>
    <%= f.text_field :amount %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :category %>
    <%= f.text_field :category %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
